[FilterPlugin]
; Files RunUAT BuildPlugin packages in addition to its defaults (Binaries, Content, Resources, Shaders, Source); paths are
; relative to the plugin root. Config/ is not a default: without this line the packaged plugin would lack the
; [CoreRedirects] that keep saved FogMS Box values across the W38 property renames.
/Config/DefaultMultiLobeSpec.ini
