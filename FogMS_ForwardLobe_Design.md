<!-- Дизайн-заметка, 2026-09-24, проверено по HEAD 74a5c1c (ветка claude/fogms-production). Код не менялся. -->

# FogMS: прямой лепесток (forward lobe) и мягкое солнце вместо SSFS

Метки: **V** — проверено по файлу/логу; **A** — предположение; **M** — нужно измерить (замер в срезе F0).
Логи — `E:\GITHUB\MultiLobeSpec\.codex-build\`, результаты проб — `Tools/FogMSEnergyValidation/ProdProbe/results/`.

> **Поправка ведущего (24.09):** SSFS по решению владельца **выключен по умолчанию**: стартовый пресет проекта `Saved/FogMS/enable_box.py` переключён на `r.FogMS.SSFS 0` (копия старого — `enable_box.py.bak_20260924_ssfs1`). Решение 5.3 закрыто: SSFS — только опция для сравнения. Фаза тумана 0,6–0,8 в гибриде дала у владельца чёрные блоки внутри облака (диагностика `ProdProbe/results/diag34`), поэтому путь «глобальная фаза тумана» (2.C.2, решение 5.2) под вопросом до разбора; путь F1 (лепесток в материале Box) от глобальной фазы не зависит.

## 0. Коротко

- **Эталон 22.09 тоже был изотропным.** Overlay-доставка требовала у тумана `Scattering Distribution = 0`; всё видозависимое на той картинке делал экранный проход SSFS.
- **SSFS не удалён:** код тот же, стартовый пресет проекта его включал, в режиме инъекции он работает; 24.09 его дважды выключали из консоли.
- **Главная причина «плоско и серо» — тонкая среда:** коэффициент многократного рассеяния (Total/Primary) сейчас 3,5, при настройках как 22.09 — 12–14. Тёплое свечение изнутри — это и есть многократное рассеяние, в тонкой среде ему неоткуда взяться.
- Прочие различия: солнце теперь после атмосферы (слабее и теплее), фаза тумана, вероятно, 0 (M), небо из LUT; мелкие — 16 направлений вместо 96, инъекция вместо overlay.
- Два дешёвых дополнения, по умолчанию выключены: **A** — прямой лепесток многократного рассеяния в материале Box (данные уже в поле, решатель не меняется); **B** — `Sun Softness` (те же лучи тени солнца, разбросанные в конусе). Вместе < 0,1 мс. Толщину среды они не заменяют.
- Порядок: замер F0 → F1/F2.

## 1. Почему картинка плоская

### 1.1 Что было 22.09 (эталон)

- **Сборка:** коммит `f65e87e` (22.09, влит в main как `340fa7a`); пакеты `FogMS_ViewIntegration_20260922/Package6` и `FogMS_SSFS_FogLight_20260922/Package1` (SSFS по полному HDR) (V).
- **Итоговый пресет того дня** (`FogMS_SSFS_FogLight_Fix.md`, V): `r.FogMS.ViewIntegration 0` (режим 3 стоил 16–77 мс), `r.FogMS.SSFS 1`, `Amount 1`, `Radius 24`, штатный FSSS выключен, сетка тумана 4 px × 208 слоёв, история 0,9.
- **Решатель:** B3, 96 направлений / 8 итераций, холодный старт (warm start появился 23.09, `eb43a2b`); 96/16 холодный = 26,4 мс только на перенос. Отсюда «20–30 мс» на кадр (A).
- **Доставка — overlay:** штатное однократное рассеяние в Box заменялось изотропным полем J, поэтому фаза тумана обязана была быть 0 (`FogMS_BoxRuntime.cpp:1186-1198`; снимок сцены `volumetric_fog_scattering_distribution 0.0`, V).
- **Параметры сцены** (снимок `FogMS_Motion_20260921/scene-inspect-20260921T212216_072687Z-c952cf64.json`): `Density 0.5`, `Threshold 0.5`, `Softness 0.1`, `DetailStrength 0.1`, `DetailScale 8`, `DensityEdgeFeather 2000` см; солнце 120 лк, высота ≈ 25°. Feather именно в момент эталона — (A).
- **Солнце:** сборщик источников брал `GetColor()` — освещённость вне атмосферы (исправлено в раунде 14, `3fe0fad`, V). Солнце было ярче и белее штатного.
- **Небо:** приватный захват SkyLight с точечной выборкой; солнечный диск не исключался (двойной учёт найден в раунде 5, V).

**Что делал SSFS** (`FogMS_ScreenScatteringPost.usf`, `FogMS_ScreenScattering.cpp`):
- вход: HDR-кадр до пост-обработки, глубина, интегрированный туман (пропускание T);
- считается: пирамида размытий, уровни разделены по глубине, Karis-веса на первом уровне гасят выбросы;
- выход: `SceneColor + Amount·(1−T)·(Blur − Mass·SceneColor)` — где туман плотный, пиксель заменяется размытым окружением;
- глазом: диск солнца и яркие куски облака «растекаются» по соседним пикселям (ореол у кромок), свет освещённой стороны затекает в тень (тёплое свечение).

Ограничения SSFS: экранное приближение без сохранения энергии; маска общая для всего тумана, не только Box; приватный `FViewInfo::VolumetricFogResources` (`FogMS_ScreenScattering.cpp:122`) — блокер для Fab. Цена 0,154–0,211 мс при 1061×813, 0,123 мс при 701×473 в инъекции (V).

### 1.2 Что сейчас (раунд 33)

- **Доставка — гибрид (`FogMS_InjectionMode 2`).** Штатный туман считает однократное рассеяние солнца со своей фазой (Height Fog `Scattering Distribution`) и умножает его в каждой ячейке 32³ на S = T_sun·k. Поле несёт J − Direct_sun: небо, локальные источники, всё многократное рассеяние (`FogMS_Transport.usf:493-528`, V). Если фаза тумана 0 (M), у однократного рассеяния нет прямого лепестка вообще.
- **SSFS не вырезан:** три файла не менялись с `340fa7a` (V); пресет ставил `SSFS 1/1/24` (`Main33e.log`, V); проход работает и для инъекции (триггер `FogMS_BoxRuntime.cpp:786-820`, маркеры 5 и 6); 24.09 в 17:36 и 17:45 его выключали вручную (`Main33f.log`, V). В руководстве: `-BindlessAll` нужен только «SSFS sky disk», не самому SSFS.
- **Среда тонкая** (`medium_r33.txt`, V). Авторские значения: `Density 1.6`, `Threshold 0.55`, `Softness 0.15`, `Detail 0.2 × 2`, `DensityEdgeFeather 12000` см, профиль Cumulus. Заполнено 40 % ячеек, ядро (плотнее половины пика) 1134 из 32768 (3,5 %), Total/Primary = **3,5**. Без профиля и с feather 500: ядро 12724, **11,7**. Набор, близкий к 22.09 (порог 0,45, без detail, плотность 0,5): **14**. `fieldcheck_r33.txt`: отключение всего многократного рассеяния темнит кадр Box лишь до ×0,884. Облако выглядит как освещённая дымка; изотропия вторична.
- **Солнце после атмосферы** (раунд 14, V): при высоте ≈ 25° слабее и теплее, чем 22.09, на 15–30 % (A, M). Отношение «солнце/небо» падает — картинка сереет.
- **Небо** (раунд 25, V): при статическом SkyLight публичный захват тождествен старому; при Real Time Capture — Sky View LUT (без облаков, на 23 % темнее), но старый эталон между повторами гулял на 26 %.
- **Мелкое** (V): 16 направлений против 96/64 — поле в пределах 2,5 %; инъекция против overlay — 37,8 дБ, яркость ×0,997; сетка тумана та же.

### 1.3 Сводная таблица

| Фактор | 22.09 | Сейчас | Влияние на вид | Как исправить |
|---|---|---|---|---|
| Толщина среды | feather 2000, порог 0,5, без профиля | feather 12000, Cumulus, порог 0,55 | **главное**: MS 3,5 против 12–14 | авторские параметры (F0), без кода |
| Видозависимый член | только SSFS | SSFS (если вкл.) + фаза тумана в гибриде (если g > 0) | нет ореола и серебряной кромки | срез F1 |
| Солнце | вне атмосферы (ошибка) | после атмосферы | темнее и серее | `Volumetric Scattering Intensity` у солнца либо принять |
| Небо | захват | LUT при Real Time Capture | темнее, без облаков | `r.FogMS.World.SkySource 3` при статическом небе |
| Жёсткость солнечной тени | конуса нет | так же | резкая граница свет/тень в 32³ | срез F2 |
| Квадратура, доставка, история | 96/8, overlay | 16, инъекция, история 0,9 | 1–3 % | не трогать |

## 2. Варианты

### 2.A Прямой лепесток многократного рассеяния

**Идея.** Решатель считает J как среднее по всем направлениям, материал отдаёт его одинаково во все стороны (Emissive = σs·J). Реальное многократно рассеянное солнце «помнит» направление солнца: вперёд его больше, вбок и назад меньше. Долю поля, которая помнит солнце, перераспределяем по углу, не добавляя энергии.

**Что входит** (всё уже есть):
1. Альфа поля `FogMS_TransportField`, в гибриде A = 0,5 + 0,5·S, S = T_sun·k (`FogMS_Transport.usf:516-522`). Ночью k → 0 ⇒ лепесток сам выключается.
2. Направление взгляда `CameraVector` (материал считается в каждом фрокселе, `MaterialTemplate.ush:4702`, V).
3. Направление на солнце — штатный узел `SkyAtmosphereLightDirection` (индекс 0, публичный, `View.AtmosphereLightDirection[0]` — тот же источник, что берёт решатель, V).
4. Три скаляра MID: `FogMS_ForwardStrength` s (0–1, по умолчанию 0), `FogMS_ForwardG` g (0–0,9, предлагаю 0,6), `FogMS_ForwardDepth` b (0,05–1, предлагаю 0,5). Свойства Box `Forward Scattering`, `Forward Anisotropy`, `Forward Depth` (`FogMS|Look`). Строка пакета не нужна.

**Что считается** — две «октавы» в духе Wrenninge 2013 / Hillaire 2016 (a = ½, c = ½; нулевая октава — однократное рассеяние, в гибриде его считает штатный туман):

```hlsl
// FogMS_ForwardLobe: множитель к Emissive инъекции; среднее по всем направлениям взгляда = 1.
float S  = max(saturate(2.0f * FieldA - 1.0f), 1e-6f);        // T_sun*k из альфы поля
if (dot(SunDir, SunDir) < 0.25f) return 1.0f;                  // нет атмосферного солнца
float mu = dot(normalize(SunDir), -CameraVector);              // 1 = взгляд прямо на солнце
float f1 = Strength * (2.0f/3.0f) * pow(S, Depth);             // октава 1: a/(a+a^2), T^b
float f2 = Strength * (1.0f/3.0f) * pow(S, Depth * Depth);     // октава 2: a^2/(a+a^2), T^(b^2)
float g1 = G, g2 = 0.5f * G;                                    // c = 1/2
float h1 = 1 + g1*g1 - 2*g1*mu, h2 = 1 + g2*g2 - 2*g2*mu;
float p1 = (1 - g1*g1) * rsqrt(h1*h1*h1);                       // 4*pi*HG, среднее по сфере = 1
float p2 = (1 - g2*g2) * rsqrt(h2*h2*h2);
return 1.0f + f1 * (p1 - 1.0f) + f2 * (p2 - 1.0f);
```

**Что выходит.** Emissive = (прежний Emissive инъекции) × Lobe. Прежний узел не меняется; новый Custom-узел `FogMS_ForwardLobe` умножает его результат. Выбор — static switch `FogMS_ForwardLobe` (по умолчанию false) в `M_FogMS_Density` и MIC `MI_FogMS_Density_Forward`; Box берёт MIC родителем MID только при s > 0 и активном гибриде. С выключенным switch HLSL прежний — картинка по умолчанию та же в пределах шума кадра.

**Интуиция** (p̂ = 4π·HG): g = 0,6 — на солнце ×10, 15° от солнца ×7, вбок ×0,40, от солнца ×0,16; g = 0,8 — на солнце ×45, от солнца ×0,06. При s = 0,5 и S ≈ 1 освещённая сторона (солнце за спиной) темнеет на 30–40 % доли многократного рассеяния, контровая кромка и облако вокруг солнца — в разы ярче; в глубине облака свет остаётся изотропным. Это лепесток **внутри** облака; ореол поверх неба за силуэтом, как у SSFS, он не рисует.

**Энергия.** Среднее Lobe по направлениям = 1 (энергия поля не меняется), Lobe ≥ 1 − s ≥ 0. Режим 2: лепесток только к J_ms, однократное рассеяние штатное — двойного счёта нет (приближение: лепесток падает и на нерассеянное небо в J_ms, вес k это подавляет). Режим 1: сегодня A = 1, поэтому лепесток только в срезе F1b (решатель пишет в альфу 0,5 + 0,5·S); тогда J содержит и однократное рассеяние — серебряная кромка с фазой самого Box, независимо от глобальной фазы тумана. Overlay не затрагивается. SSFS независим — сравнивать при `SSFS 0`.

**Цена.** Решатель в режиме 2: 0 мс. Материал ≈ 25 ALU на фроксел Box в `VolumetricFog::VoxelizePrimitives`, ≤ 0,05 мс (A, M). F1b: +≈ 0,02 мс.

**Риски.** (1) `SkyAtmosphereLightDirection` в вокселизации — проверить компиляцию; запасной путь — MID-вектор `FogMS_SunDirection`. (2) Узкий пик при g ≥ 0,8 — «светлячки» у солнца; g ≤ 0,9, проверить TAA. (3) Амплитуду задаёт 32³. (4) Смена родителя MID сбрасывает параметры — проверить, что нет кадра с пустым MID.

### 2.B Мягкое солнце: `Sun Softness`

**Идея.** Проход 2 для каждой ячейки стреляет 4 (или 8) лучей к солнцу (`DirectShadow` + `MediumT`), все строго на солнце. Разбросаем их в конусе θ, не меняя числа лучей: граница свет/тень внутри облака и тени от геометрии мягчеют.

**Входит:** свойство Box `Sun Softness` (0–15°, по умолчанию 0) → строка пакета **29.x = tan θ** (27–28 зарезервированы под weather map и рельеф). Решатель уже получает все 32 строки (`BoxRows`); смена строки меняет ревизию — поле пересчитывается сразу, warm start валиден.

**Считается** (проход 2, `FogMS_Transport.usf:300-358`), для солнца при tanθ > 0:

```hlsl
// Подточка Sample (0..7): 8-точечный диск Фогеля, r = tanθ·sqrt((Sample+0.5)/8), φ = Sample·2.39996,
// D' = normalize(D + r·(cosφ·T1 + sinφ·T2)). DirectSamples 4 чередует {0,3,5,6}/{7,4,2,1} по чётности решения.
```

`DirectShadow`, `MediumT`, T_sun и Direct_sun получают один D' — поле, T_sun и вычитаемое в гибриде согласованы.

**Выходит:** Direct мягче; в гибриде мягче и S (значит, и штатное однократное рассеяние в Box); штатные карты теней остаются резкими. Энергия солнца не меняется, усредняется видимость; ожидаемая глобальная прибавка ≤ +3 % (M).

**F2b (опционально):** размытие Direct и T_sun ядром 3³ с нулевым весом пустых ячеек; риск протечки сквозь тонкие стены (ячейка 3–7 м).

**Цена:** проход 2 сейчас 0,107 мс → 0,11–0,15 мс (M); F2b +0,01–0,02 мс и 512 КБ.

**Риски:** «копии» тени при больших θ (θ ≤ 15°); межкадровая разница от чередования (критерий ≤ 1,5 %).

### 2.C Без кода (срез F0)

1. **Толщина среды:** `DensityEdgeFeather` ≤ 2000, профиль выключен (или Cumulus с feather 500), порог ≈ 0,5; цель — MS > 10 по `mediumcheck.py`.
2. **Фаза тумана в гибриде** 0,6–0,8 — у владельца дала чёрные блоки, см. поправку.
3. **Яркость солнца:** `Volumetric Scattering Intensity` солнца умножает туман и решатель одинаково.
4. **SSFS** 0 против 1 — только для сравнения с эталоном.

### 2.D Отложено

δ-масштабирование в решателе (расходится с экстинкцией штатного тумана); фаза Box через видозависимый BaseColor (движок зажимает альбедо в [0,1], `VolumetricFogVoxelization.usf:66`); лепесток в overlay-патче (только редактор).

## 3. Рекомендуемые срезы

### F0 — замер и эталон, без кода плагина
Файлы: `ProdProbe/refab22.py`, `ProdProbe/refcmp.py`, `ProdProbe/ref22_setup.ps1` (новые), `FogMS_Prod_Report.md`.
1. Записаны фаза тумана, SSFS, солнце, авторские параметры против снимка 21.09.
2. Таблица компонент с повторами: только штатный туман / только прямой свет / гибрид / полное поле / overlay / ± SSFS / плотность как 22.09 / 96/8 холодный + `SkySource 3`.
3. `mediumcheck.py` для авторского набора и набора 22.09.
4. Сессия Package6 (4.6): слой σ двух сборок совпадает (rel L2 ≤ 1e-3).
5. Отношение J (сейчас/эталон) по трём областям; необъяснённый остаток ≤ 5 %.
6. GPU-профили обеих сборок по очереди.
7. Решения арт-лида записаны.

### F1a — лепесток в гибриде
Файлы: `FogMS_BoxVolume.h/.cpp`, `ProdProbe/matedit_forward.py` (новый), `Content/FogMS/M_FogMS_Density.uasset`, `Content/FogMS/MI_FogMS_Density_Forward.uasset` (новый), `ProdProbe/fwdab.py` (новый).
1. Сборка StrictIncludes; `matedit_forward.py` компилирует материал, повтор печатает `ALREADY_PATCHED`.
2. При s = 0: прежний родитель, HLSL вокселизации прежний, кадр в пределах шума повтора, overlay не меняется.
3. CPU: среднее Lobe по сфере = 1 ± 1e-3, минимум ≥ 0 для g ∈ {0; 0,3; 0,6; 0,8; 0,9}.
4. s = 1, g = 0,6: контровая кромка ≥ ×1,10; солнце за камерой ×0,6…×1,00; вбок ±40 % (M).
5. Облёт 12 видов: средняя разница HDR в Box ≤ ±5 %.
6. Вокселизация дороже ≤ 0,05 мс.
7. Ночь: вкл./выкл. совпадают в пределах шума.

### F1b — лепесток в режиме 1
Файлы: `FogMS_Transport.cpp`, `FogMS_Transport.usf`, `FogMS_WorldLighting.h/.cpp`, `FogMS_BoxRuntime.cpp`.
1. Без лепестка поле в пределах шума (rel L2 ≤ 2e-4). 2. RGB поля тот же J, альфа = 0,5 + 0,5·T_sun·k. 3. Проход 2 ≤ +0,03 мс. 4. `SolveInterval 2` + async: лепесток не пульсирует. 5. Контровой вид ≥ ×1,10, облёт ±5 %. 6. Статус `forward lobe`.

### F2 — `Sun Softness`
Файлы: `FogMS_BoxVolume.h/.cpp`, `FogMS_BoxRuntime.cpp`, `FogMS_Transport.usf`, `ProdProbe/softab.py` (новый).
1. При 0° пакет и ревизия прежние, поле в пределах шума. 2. При 3° яркость ×0,97…×1,03, переход тени шире. 3. Разница соседних решений ≤ 1,5 %. 4. Проход 2 ≤ 0,15 мс. 5. Под полом Direct ≤ 1 %. 6. Ночь и `DirectSamples 8` в порядке, статус `sun softness 3.0°`. 7. Визуальная оценка владельцем.

### F2b — размытие T_sun (опционально)
Выключено — в пределах шума; Σ Direct ±1 %; кромка темнеет ≤ 5 %; ≤ 0,02 мс; нет протечки.

### F-doc
`FogMS_UserGuide.md`, `FogMS_Prod_Report.md`, `FogMS_HANDOVER.md`, `CHANGELOG.md`: каждое новое свойство описано (что делает, цена, значение по умолчанию).

## 4. План проверки

- **Правила:** `r.SkyLight.RealTimeReflectionCapture 0`, `VolumetricCloud` скрыты, `manual_animation_time 100`, одна камера, каждая конфигурация повторяется (шум: 50–52 дБ по кадрам, rel L2 ≈ 2e-4 по полю).
- **Тождественность по умолчанию:** `identity_check.sh`, `delivab.py`; критерий «не хуже повтора» плюс совпадение HLSL (F1) или неизменность пакета (F2).
- **F1 — `fwdab.py`:** виды контровой / боковой / солнце за спиной; конфигурации выкл., s 0,5/g 0,6, s 1/g 0,6, s 1/g 0,8 (все с `SSFS 0`); метрики по трём ROI; облёт 12 видов; `--cpu` интегрирует Lobe по сфере.
- **F2 — `softab.py`:** θ = 0/1/3/6° с дампами, `fielddiff.py`, ширина перехода, разница соседних решений, `gpuprofile.py`.
- **Цена:** `gpuprofile.py`, сравнивать проходы, не кадр.

### 4.6 Сессия «Package6 рядом, одна камера, числа по компонентам»

- **Бинарники:** `.codex-build/FogMS_ViewIntegration_20260922/Package6` и `.codex-build/FogMS_SSFS_FogLight_20260922/Package1` (SSFS по полному HDR — вероятно, эталон). Собраны под UE 5.8.2 CL56702186; если движок не обновлялся, загрузятся (A). Иначе пересборка: `git worktree add <путь> 340fa7a` → `RunUAT BuildPlugin` в отдельный пакет.
- **В одном редакторе нельзя** (одинаковые имена плагина, модулей, `/Plugin/FogMS`) — нужна **копия проекта** (`.uproject`, `Config`, `Content/FogMS_Test` и зависимости, без `Saved/Intermediate/DerivedDataCache`), старый пакет в её `Plugins/MultiLobeSpec`.
- **Шаги:** запуск старой сборки с `-BindlessAll` и пресетом 22.09 → копия карты (никогда не сохранять обратно) с одинаковой плотностью (эрозия/профиль выкл.), солнцем и статическим SkyLight → та же камера → совпадение σ → снимки (Mode Off, `Indirect 0`, полный, ± SSFS; в текущей ещё гибрид/полное поле/overlay/F1/F2) и дампы J → разложение (96/8 холодный, `SkySource 3`; вклад солнца вне атмосферы — отношение слоёв Primary) → профили по очереди.
- **Оценка:** 2–3 часа окна редактора.

## 5. Открытые решения для арт-лида

1. Сначала вернуть толщину среды как 22.09 (feather ≈ 2000, без Cumulus), затем смотреть на лепесток? Рекомендую да.
2. Серебряная кромка в гибриде — глобальная фаза тумана (задевает весь туман; у владельца дала артефакты) или режим 1 + F1b с фазой самого Box?
3. ~~SSFS в пресете?~~ Решено 24.09: выключен по умолчанию, опция для сравнения.
4. Значения лепестка по умолчанию (s 0,5, g 0,6, b 0,5); устраивает ли, что освещённая сторона немного темнеет?
5. `Sun Softness`: диапазон 0–15°, что предлагать (2–5°)? Нужен ли F2b?
6. Солнце: принять физичное или компенсировать `Volumetric Scattering Intensity`?
7. Эталон: Package6 или FogLight Package1? Камера и время суток?
