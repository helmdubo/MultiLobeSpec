# FogMS Production — этап C: warm start, бюджет итераций, малые квадратуры

Дата: 2026-09-23. Ветка `claude/fogms-production` (от `main` 340fa7a). UE 5.8.2, D3D12/SM6/BindlessAll, RTX 3070, карта `/Game/FogMS_Test/FogMS_Box`, Box 32³. Все числа ниже измерены на этой сцене через UE-MCP мост (скрипты `Tools/FogMSEnergyValidation/ProdProbe/`). Это измерения, не визуальная приёмка владельца.

## Что изменено

1. **Warm start PCG** (`r.FogMS.Transport.WarmStart`, default 1). Начальное приближение u₀ = √σs · J_prev из атласа предыдущего кадра того же Box; невязка r₀ = b − A·u₀ считается честным применением оператора (один дополнительный sweep); опорная величина для критерия остановки остаётся холодной (`Scalars[3]` = ⟨b, b·D⁻¹⟩). Это продолжение итерации неподвижной точки между кадрами, не смешивание с историей. Инвалидация: любое изменение геометрии Box, `ResetHistory`, смена диагностических cvar, режим reconstruction test. Смена числа направлений и итераций warm start не сбрасывает (J не зависит от квадратуры).
2. **`r.FogMS.Transport.Tolerance`** (default 1e-14 = прежнее поведение). Относительный порог rho, ниже которого оставшиеся матричные проходы кадра пропускаются. Вместе с warm start это адаптивный бюджет: «до N итераций, пока не сошлось».
3. **Transport Iterations** теперь [1, 64] (было [4, 64]).
4. **Angular Quality: 16 и 24 направления** (2 полярных × 4/6 азимутальных × 2 полусферы) в дополнение к 48/96.
5. **Ранний выход detail-шума** в `FogMS_IndirectLocalDensity`: вне полосы неопределённости порога detail-октавы не читаются (8 загрузок вместо 24), вторая октава не читается при нулевом весе. Точность доказана (|Δ| ≤ DetailStrength).

Файлы: `FogMS_Transport.cpp/.h/.usf`, `FogMS_WorldLighting.cpp/.h`, `FogMS_BoxRuntime.cpp`, `FogMS_BoxVolume.h`, `FogMS_Indirect.ush`.

## Измерения

Метрика сходимости: `FogMS.DumpSpatial` → слой 0 атласа, `weighted` = Σ err·L(J) / Σ L(J) (невязка ячейки, взвешенная её яркостью); `max` = худшая ячейка (шумит из-за тусклых ячеек, приведена для справки). GPU — `ProfileGPU`, сумма проходов транспорта.

| Конфигурация | Транспорт, мс | Кадр GPU, мс | weighted | max |
|---|---|---|---|---|
| Эталон: 96 напр., 64 ит., холодный | 73,7 | 85,0 | 0,000000 | 0,00016 |
| Прежний preset: 96 / 16, холодный | 26,4 | 36,8 | 0,00032 | 0,078 |
| 96 / 16, warm | 28,1 | 38,3 | 0,000000 | 0,0012 |
| 96 / 4, warm | 11,1 | 21,3 | 0,00002 | 0,0030 |
| 96 / 2, warm | 8,5 | 18,8 | 0,00007 | 0,013 |
| 48 / 4, warm | 6,9 | 17,5 | 0,00003 | 0,017 |
| 24 / 4, warm | 4,4 | 15,0 | 0,00002 | 0,014 |
| **16 / 4, warm** | **3,3** | **13,9** | **0,00002** | 0,0087 |
| 16 / 2, warm | 2,5 | 13,1 | 0,00006 | 0,0091 |
| 16 / ≤16, tol 1e-6, warm | 3,4 | 14,5 | 0,00003 | 0,023 |
| 16 / ≤16, tol 1e-8, warm | 5,2 | 16,3 | 0,000004 | 0,034 |
| 16 / ≤16, tol 1e-4, warm | 2,3 | 13,3 | 0,00025 | 0,035 |

Выводы:
- Warm start при той же стоимости (96/16) снижает взвешенную невязку с 3,2e-4 до <1e-6: прежний preset за кадр не сходился, теперь решение переносится между кадрами.
- 16 направлений с warm start дают невязку уровня 96/4 за треть цены. PSNR скриншотов всех warm-конфигураций относительно эталона 49,5–49,9 дБ (различия в этом ракурсе на уровне шума TAA); визуальную оценку кромок и цвета делает владелец.
- Допуск 1e-4 слишком груб (глобальная норма 1 %, тусклые ячейки до 3–8 %), 1e-6 — рабочий, 1e-8 — high.

### Отклик на изменение света (солнце 140 → 0 → 140), 16 направлений

| Момент | Фикс. 4 ит./кадр, weighted | Адаптивно ≤16, tol 1e-6, weighted |
|---|---|---|
| до выключения | 0,00002 | 0,00002 |
| сразу после | 0,00207 | 0,00002 |
| +0,5 с | 0,00009 | 0,00002 |
| +1 с | 0,00001 | 0,00002 |
| включение, сразу | 0,00134 | 0,00002 |
| +1 с | 0,00003 | 0,00002 |

Адаптивный бюджет восстанавливает решение в том же кадре при равной стоимости в покое. Фоновый уровень 1–3e-5 создают анимация плотности и покадровый шум источников (Lumen surface cache, джиттер), это не остаток итераций.


## Квадратура, ориентированная на солнце: A/B на замороженной плотности

Методика: `freeze_density_animation`, одна фаза для всех конфигураций; эталон 96 направлений / 64 итерации / холодный старт / осевой набор; сравнение полей J (слой 0 атласа) по взвешенной по яркости относительной разнице.

| Конфигурация | Транспорт, мс | rel L2 к эталону | отношение яркости |
|---|---|---|---|
| 96/64 холодный, **sun-aligned** | 78,0 | 0,062 | 1,027 |
| 16 / ≤16 / 1e-6, осевой | 2,33 | 0,099 | 0,949 |
| 16 / ≤16 / 1e-6, **sun-aligned** | 2,33 | **1,122** | **1,972** |
| 24 / ≤16 / 1e-6, осевой | 2,85 | 0,101 | 1,066 |
| 24 / ≤16 / 1e-6, sun-aligned | 2,86 | 0,525 | 1,415 |
| 48 / ≤16 / 1e-6, осевой | 4,04 | 0,054 | 0,965 |
| 48 / ≤16 / 1e-6, sun-aligned | 4,07 | 0,428 | 1,391 |

Вывод: ориентация на солнце в текущем виде — регрессия. Причина — двойной учёт солнечного диска: `FogMS_WorldSky` сэмплит захваченную кубмапу Sky Light, в которой есть диск; ордината, смотрящая на солнце, получает радианс диска с весом целого сектора квадратуры, тогда как прямое солнце уже учтено проходом direct с тенями. Осевые наборы попадали в диск редко и случайно, поэтому ошибка была скрыта (но присутствовала: даже эталон меняется на 2,7 %). Исправление в работе: исключение конуса вокруг солнца из граничного радианса неба (`r.FogMS.World.SunExcludeDegrees`). До исправления `r.FogMS.Transport.SunAligned` следует держать в 0.

### После исключения солнечного диска (`r.FogMS.World.SunExcludeDegrees 3`, round 5)

| Конфигурация | rel L2 к эталону | отношение яркости |
|---|---|---|
| 96/64 холодный, осевой, исключение 0° (прежнее поведение) | 0,0002 | 1,000 |
| 96/64 холодный, sun-aligned | 0,051 | 0,956 |
| 16 / ≤16 / 1e-6, осевой | 0,118 | 0,931 |
| 16 / ≤16 / 1e-6, sun-aligned | 0,443 | 1,368 |
| 24 / ≤16 / 1e-6, осевой | 0,090 | 1,069 |
| 24 / ≤16 / 1e-6, sun-aligned | 0,104 | 1,042 |
| 48 / ≤16 / 1e-6, осевой | 0,066 | 0,955 |
| 48 / ≤16 / 1e-6, sun-aligned | 0,218 | 1,199 |

Для осевых наборов исключение диска ничего не меняет (они в диск не попадали), для sun-aligned ошибка уменьшилась вдвое, но осталась. Оставшаяся причина — точечная выборка неба в центре сектора квадратуры: ордината у солнца видит яркий атмосферный ореол и переносит его на весь сектор (для 16 направлений сектор ~58°). Следующий шаг — префильтрация неба по размеру сектора (mip кубмапы, равный угловому размеру сектора). До этого `r.FogMS.Transport.SunAligned` по умолчанию 0.

### После префильтрации неба по сектору квадратуры (round 6, `r.FogMS.World.SkyMipBias 0`)

| Конфигурация | Транспорт, мс | rel L2 к эталону | отношение яркости |
|---|---|---|---|
| 96/64 холодный, осевой, точечная выборка неба (прежнее поведение) | 67,9 | 0,066 | 1,010 |
| 96/64 холодный, sun-aligned | 77,6 | **0,004** | 1,000 |
| 16 / ≤16 / 1e-6, осевой | 2,25 | 0,031 | 0,977 |
| 16 / ≤16 / 1e-6, **sun-aligned** | 2,26 | **0,025** | 0,991 |
| 24 / ≤16 / 1e-6, осевой | 2,74 | 0,014 | 0,994 |
| 24 / ≤16 / 1e-6, sun-aligned | 2,75 | 0,012 | 0,998 |
| 48 / ≤16 / 1e-6, осевой | 3,90 | 0,008 | 0,995 |
| 48 / ≤16 / 1e-6, sun-aligned | 3,94 | 0,008 | 0,998 |

Выводы:
- Префильтрация неба устранила зависимость от ориентации: при 96 направлениях повёрнутый и осевой наборы совпадают до 0,4 % (было 5 %). Прежняя точечная выборка сама вносила ~4–7 % ошибки в эталон.
- 16 направлений теперь в пределах 2,5–3 % от эталона 96/64 (было 10–12 %), 24 — 1,2 %, 48 — 0,8 %. Ориентация на солнце при 16 направлениях помогает (3,1 % → 2,5 %), поэтому `r.FogMS.Transport.SunAligned` снова 1 по умолчанию.
- Оставшаяся ошибка малых наборов — дискретизация переноса внутри среды, не граница.

Также по замороженным данным (до префильтрации): 16 осевых направлений с адаптивным бюджетом дают поле в пределах 10 % (L2) от эталона 96/64 за 2,3 мс; 48 — в пределах 5,4 % за 4 мс. Это угловая ошибка дискретизации, не недосходимость.

## Рекомендуемые тиры (предложение, не применено к дефолтам)

| Тир | Направления | Итерации | Tolerance | Ожидаемая стоимость (эта сцена) |
|---|---|---|---|---|
| Production | 16 (sun-aligned) | ≤16 (адаптивно) | 1e-6 | ~2,3–3,5 мс |
| High | 24–48 | ≤16 | 1e-8 | 5–8 мс |
| Cinematic | 96 | 64 | 1e-14 | 27–74 мс |

Штатный туман при preset 4 px / 208 слоёв стоит ещё ~5–6 мс; production-режим предполагает 8–16 px.

## Раунд 7: доставка через emissive volume-материала (C7a) — A/B против overlay

Что сделано (коммиты `0d87392`, `6c8e2a6`): у Box появилась опция **Emissive Injection** (по умолчанию выключена).
Поток данных: transport-решатель → новый проход 17 публикует J (32³, FloatRGBA, alpha 1 = поле валидно) в
`UTextureRenderTargetVolume`, принадлежащий Box → volume-материал Box читает его как `FogMS_TransportField`
и пишет `Emissive = Mode·Field.a·J·Albedo·σt`, `BaseColor = Albedo·(1 − Mode·Field.a)` → штатная воксели-
зация UE кладёт σs·J в VBufferB, а overlay для этого Box не инжектирует ни плотность, ни источник
(маркер пакета 23.w = 5; все читатели overlay требуют ровно 4). Кадр без публикации очищает поле (alpha 0) —
материал возвращается к штатному освещению по альбедо.

Измерение: та же сцена, замороженная плотность, 16 направлений (sun-aligned), ≤16 итераций, tolerance 1e-6,
`abinject.sh`, редактор на переднем плане, лог `Main7.log`.

| Метрика | Overlay (23.w = 4) | Emissive Injection (23.w = 5) |
|---|---|---|
| GPU-кадр | 11,55 мс | 10,76 мс |
| FogMS transport (B3, 16 напр.) | 2,35 мс | 2,33 мс |
| Native `VolumetricFog` (родитель) | 4,00 мс | 3,45 мс |
| — `LightScattering` 176×119×208 SS:4 | 1,575 мс | 0,870 мс |
| — `RaytraceDirLightShadow` | 2,11 мс | 2,26 мс |
| — `FinalIntegration` | 0,233 мс | 0,233 мс |
| Поле J (rel L2 к overlay-прогону) | — | 2·10⁻⁴ (тот же решатель) |
| PSNR внутри передней грани Box | — | 37,8 дБ, средняя яркость ×0,997 |
| PSNR полного кадра | — | 27,5 дБ (небо анимировано: только небо — 19 дБ) |

Что это значит по потоку данных:
- Выигрыш ~0,7 мс — это исчезнувшая реконструкция источника в `LightScatteringCS` (4 тетраэдральные выборки
  плотности + связность stencil на фроксел). Штатная вокселизация читает то же поле аппаратной трилинейкой.
- Картинка внутри Box практически совпадает (разница сосредоточена на силуэте тумана и у пола — там, где
  overlay-реконструкция учитывает связность, а штатная трилинейка — нет). Полнокадровый PSNR не показателен:
  облака в skybox движутся между двумя снимками.
- Статус актора при инъекции: `Active B3 isotropic transport (16 directions; emissive injection via material; …)`.

Что этот A/B **не** доказывает: временное поведение (штатная история 0,9 и джиттер теперь применяются к
σs·J этого Box — отклик на смену света станет плавнее и медленнее, чем у overlay-пути), фейд после 60 %
дальности штатной вокселизации, и то, что сборка без `-BindlessAll` уже возможна — решатель по-прежнему
читает атлас плотности через bindless; инъекция снимает bindless только с потребителя (см.
`FogMS_BindlessFree_Research.md`). Визуальную оценку делает владелец проекта.

Файлы: `Tools/FogMSEnergyValidation/ProdProbe/results/abinject_r7.txt`, `results/r7_inject/*_gpuprofile.txt`.

## Что не сделано / открыто

- Квадратура, ориентированная на солнце: работает после исключения диска и префильтрации неба, включена по умолчанию.
- Кэш связности stencil и переработка `AngularSweepCS` (одна группа на направление) — P2 аудитора, не начаты.
- Требование `-BindlessAll` и crash-guard — исследование завершено (`FogMS_BindlessFree_Research.md`). Доставка через emissive volume-материала реализована как опция Box (раунд 7, см. выше): поле совпадает, LightScattering дешевле на ~0,7 мс; остаётся перевести входы решателя (атлас плотности, публикация) с bindless на обычные привязки, чтобы снять `-BindlessAll` целиком.
- Спайк при остановке камеры в редакторе: причина найдена (hit proxy + синхронное чтение GPU, ~87 мс базово + слив GPU), в игре отсутствует; уменьшается пропорционально GPU-кадру.
