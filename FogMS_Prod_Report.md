# FogMS Production — этап C: warm start, бюджет итераций, малые квадратуры

Дата: 2026-09-23. Ветка `claude/fogms-production` (от `main` 340fa7a). UE 5.8.2, D3D12/SM6/BindlessAll, RTX 3070, карта `/Game/FogMS_Test/FogMS_Box`, Box 32³. Все числа ниже измерены на этой сцене через UE-MCP мост (скрипты `Tools/FogMSEnergyValidation/ProdProbe/`). Это измерения, не визуальная приёмка владельца.

## Что изменено

1. **Warm start PCG** (`r.FogMS.Transport.WarmStart`, default 1). Начальное приближение u₀ = √σs · J_prev из атласа предыдущего кадра того же Box; невязка r₀ = b − A·u₀ считается честным применением оператора (один дополнительный sweep); опорная величина для критерия остановки остаётся холодной (`Scalars[3]` = ⟨b, b·D⁻¹⟩). Это продолжение итерации неподвижной точки между кадрами, не смешивание с историей. Инвалидация: любое изменение геометрии Box, `ResetHistory`, смена диагностических cvar, режим reconstruction test. Смена числа направлений и итераций warm start не сбрасывает (J не зависит от квадратуры).
2. **`r.FogMS.Transport.Tolerance`** (default 1e-14 = прежнее поведение). Относительный порог rho, ниже которого оставшиеся матричные проходы кадра пропускаются. Вместе с warm start это адаптивный бюджет: «до N итераций, пока не сошлось».
3. **Transport Iterations** теперь [1, 64] (было [4, 64]).
4. **Angular Quality: 16 и 24 направления** (2 полярных × 4/6 азимутальных × 2 полусферы) в дополнение к 48/96.
5. **Ранний выход detail-шума** в `FogMS_IndirectLocalDensity`: вне полосы неопределённости порога detail-октавы не читаются (8 загрузок вместо 24), вторая октава не читается при нулевом весе. Точность доказана (|Δ| ≤ DetailStrength).

Файлы: `FogMS_Transport.cpp/.h/.usf`, `FogMS_WorldLighting.cpp/.h`, `FogMS_BoxRuntime.cpp`, `FogMS_BoxVolume.h`, `FogMS_Indirect.ush`.

## Измерения

Метрика сходимости: `FogMS.DumpSpatial` → слой 0 атласа, `weighted` = Σ err·L(J) / Σ L(J) (невязка ячейки, взвешенная её яркостью); `max` = худшая ячейка (шумит из-за тусклых ячеек, приведена для справки). GPU — `ProfileGPU`, сумма проходов транспорта.

| Конфигурация | Транспорт, мс | Кадр GPU, мс | weighted | max |
|---|---|---|---|---|
| Эталон: 96 напр., 64 ит., холодный | 73,7 | 85,0 | 0,000000 | 0,00016 |
| Прежний preset: 96 / 16, холодный | 26,4 | 36,8 | 0,00032 | 0,078 |
| 96 / 16, warm | 28,1 | 38,3 | 0,000000 | 0,0012 |
| 96 / 4, warm | 11,1 | 21,3 | 0,00002 | 0,0030 |
| 96 / 2, warm | 8,5 | 18,8 | 0,00007 | 0,013 |
| 48 / 4, warm | 6,9 | 17,5 | 0,00003 | 0,017 |
| 24 / 4, warm | 4,4 | 15,0 | 0,00002 | 0,014 |
| **16 / 4, warm** | **3,3** | **13,9** | **0,00002** | 0,0087 |
| 16 / 2, warm | 2,5 | 13,1 | 0,00006 | 0,0091 |
| 16 / ≤16, tol 1e-6, warm | 3,4 | 14,5 | 0,00003 | 0,023 |
| 16 / ≤16, tol 1e-8, warm | 5,2 | 16,3 | 0,000004 | 0,034 |
| 16 / ≤16, tol 1e-4, warm | 2,3 | 13,3 | 0,00025 | 0,035 |

Выводы:
- Warm start при той же стоимости (96/16) снижает взвешенную невязку с 3,2e-4 до <1e-6: прежний preset за кадр не сходился, теперь решение переносится между кадрами.
- 16 направлений с warm start дают невязку уровня 96/4 за треть цены. PSNR скриншотов всех warm-конфигураций относительно эталона 49,5–49,9 дБ (различия в этом ракурсе на уровне шума TAA); визуальную оценку кромок и цвета делает владелец.
- Допуск 1e-4 слишком груб (глобальная норма 1 %, тусклые ячейки до 3–8 %), 1e-6 — рабочий, 1e-8 — high.

### Отклик на изменение света (солнце 140 → 0 → 140), 16 направлений

| Момент | Фикс. 4 ит./кадр, weighted | Адаптивно ≤16, tol 1e-6, weighted |
|---|---|---|
| до выключения | 0,00002 | 0,00002 |
| сразу после | 0,00207 | 0,00002 |
| +0,5 с | 0,00009 | 0,00002 |
| +1 с | 0,00001 | 0,00002 |
| включение, сразу | 0,00134 | 0,00002 |
| +1 с | 0,00003 | 0,00002 |

Адаптивный бюджет восстанавливает решение в том же кадре при равной стоимости в покое. Фоновый уровень 1–3e-5 создают анимация плотности и покадровый шум источников (Lumen surface cache, джиттер), это не остаток итераций.


## Квадратура, ориентированная на солнце: A/B на замороженной плотности

Методика: `freeze_density_animation`, одна фаза для всех конфигураций; эталон 96 направлений / 64 итерации / холодный старт / осевой набор; сравнение полей J (слой 0 атласа) по взвешенной по яркости относительной разнице.

| Конфигурация | Транспорт, мс | rel L2 к эталону | отношение яркости |
|---|---|---|---|
| 96/64 холодный, **sun-aligned** | 78,0 | 0,062 | 1,027 |
| 16 / ≤16 / 1e-6, осевой | 2,33 | 0,099 | 0,949 |
| 16 / ≤16 / 1e-6, **sun-aligned** | 2,33 | **1,122** | **1,972** |
| 24 / ≤16 / 1e-6, осевой | 2,85 | 0,101 | 1,066 |
| 24 / ≤16 / 1e-6, sun-aligned | 2,86 | 0,525 | 1,415 |
| 48 / ≤16 / 1e-6, осевой | 4,04 | 0,054 | 0,965 |
| 48 / ≤16 / 1e-6, sun-aligned | 4,07 | 0,428 | 1,391 |

Вывод: ориентация на солнце в текущем виде — регрессия. Причина — двойной учёт солнечного диска: `FogMS_WorldSky` сэмплит захваченную кубмапу Sky Light, в которой есть диск; ордината, смотрящая на солнце, получает радианс диска с весом целого сектора квадратуры, тогда как прямое солнце уже учтено проходом direct с тенями. Осевые наборы попадали в диск редко и случайно, поэтому ошибка была скрыта (но присутствовала: даже эталон меняется на 2,7 %). Исправление в работе: исключение конуса вокруг солнца из граничного радианса неба (`r.FogMS.World.SunExcludeDegrees`). До исправления `r.FogMS.Transport.SunAligned` следует держать в 0.

### После исключения солнечного диска (`r.FogMS.World.SunExcludeDegrees 3`, round 5)

| Конфигурация | rel L2 к эталону | отношение яркости |
|---|---|---|
| 96/64 холодный, осевой, исключение 0° (прежнее поведение) | 0,0002 | 1,000 |
| 96/64 холодный, sun-aligned | 0,051 | 0,956 |
| 16 / ≤16 / 1e-6, осевой | 0,118 | 0,931 |
| 16 / ≤16 / 1e-6, sun-aligned | 0,443 | 1,368 |
| 24 / ≤16 / 1e-6, осевой | 0,090 | 1,069 |
| 24 / ≤16 / 1e-6, sun-aligned | 0,104 | 1,042 |
| 48 / ≤16 / 1e-6, осевой | 0,066 | 0,955 |
| 48 / ≤16 / 1e-6, sun-aligned | 0,218 | 1,199 |

Для осевых наборов исключение диска ничего не меняет (они в диск не попадали), для sun-aligned ошибка уменьшилась вдвое, но осталась. Оставшаяся причина — точечная выборка неба в центре сектора квадратуры: ордината у солнца видит яркий атмосферный ореол и переносит его на весь сектор (для 16 направлений сектор ~58°). Следующий шаг — префильтрация неба по размеру сектора (mip кубмапы, равный угловому размеру сектора). До этого `r.FogMS.Transport.SunAligned` по умолчанию 0.

### После префильтрации неба по сектору квадратуры (round 6, `r.FogMS.World.SkyMipBias 0`)

| Конфигурация | Транспорт, мс | rel L2 к эталону | отношение яркости |
|---|---|---|---|
| 96/64 холодный, осевой, точечная выборка неба (прежнее поведение) | 67,9 | 0,066 | 1,010 |
| 96/64 холодный, sun-aligned | 77,6 | **0,004** | 1,000 |
| 16 / ≤16 / 1e-6, осевой | 2,25 | 0,031 | 0,977 |
| 16 / ≤16 / 1e-6, **sun-aligned** | 2,26 | **0,025** | 0,991 |
| 24 / ≤16 / 1e-6, осевой | 2,74 | 0,014 | 0,994 |
| 24 / ≤16 / 1e-6, sun-aligned | 2,75 | 0,012 | 0,998 |
| 48 / ≤16 / 1e-6, осевой | 3,90 | 0,008 | 0,995 |
| 48 / ≤16 / 1e-6, sun-aligned | 3,94 | 0,008 | 0,998 |

Выводы:
- Префильтрация неба устранила зависимость от ориентации: при 96 направлениях повёрнутый и осевой наборы совпадают до 0,4 % (было 5 %). Прежняя точечная выборка сама вносила ~4–7 % ошибки в эталон.
- 16 направлений теперь в пределах 2,5–3 % от эталона 96/64 (было 10–12 %), 24 — 1,2 %, 48 — 0,8 %. Ориентация на солнце при 16 направлениях помогает (3,1 % → 2,5 %), поэтому `r.FogMS.Transport.SunAligned` снова 1 по умолчанию.
- Оставшаяся ошибка малых наборов — дискретизация переноса внутри среды, не граница.

Также по замороженным данным (до префильтрации): 16 осевых направлений с адаптивным бюджетом дают поле в пределах 10 % (L2) от эталона 96/64 за 2,3 мс; 48 — в пределах 5,4 % за 4 мс. Это угловая ошибка дискретизации, не недосходимость.

## Рекомендуемые тиры (предложение, не применено к дефолтам)

| Тир | Направления | Итерации | Tolerance | Ожидаемая стоимость (эта сцена) |
|---|---|---|---|---|
| Production | 16 (sun-aligned) | ≤16 (адаптивно) | 1e-6 | ~2,3–3,5 мс |
| High | 24–48 | ≤16 | 1e-8 | 5–8 мс |
| Cinematic | 96 | 64 | 1e-14 | 27–74 мс |

Штатный туман при preset 4 px / 208 слоёв стоит ещё ~5–6 мс; production-режим предполагает 8–16 px.

## Раунд 7: доставка через emissive volume-материала (C7a) — A/B против overlay

Что сделано (коммиты `0d87392`, `6c8e2a6`): у Box появилась опция **Emissive Injection** (по умолчанию выключена).
Поток данных: transport-решатель → новый проход 17 публикует J (32³, FloatRGBA, alpha 1 = поле валидно) в
`UTextureRenderTargetVolume`, принадлежащий Box → volume-материал Box читает его как `FogMS_TransportField`
и пишет `Emissive = Mode·Field.a·J·Albedo·σt`, `BaseColor = Albedo·(1 − Mode·Field.a)` → штатная воксели-
зация UE кладёт σs·J в VBufferB, а overlay для этого Box не инжектирует ни плотность, ни источник
(маркер пакета 23.w = 5; все читатели overlay требуют ровно 4). Кадр без публикации очищает поле (alpha 0) —
материал возвращается к штатному освещению по альбедо.

Измерение: та же сцена, замороженная плотность, 16 направлений (sun-aligned), ≤16 итераций, tolerance 1e-6,
`abinject.sh`, редактор на переднем плане, лог `Main7.log`.

| Метрика | Overlay (23.w = 4) | Emissive Injection (23.w = 5) |
|---|---|---|
| GPU-кадр | 11,55 мс | 10,76 мс |
| FogMS transport (B3, 16 напр.) | 2,35 мс | 2,33 мс |
| Native `VolumetricFog` (родитель) | 4,00 мс | 3,45 мс |
| — `LightScattering` 176×119×208 SS:4 | 1,575 мс | 0,870 мс |
| — `RaytraceDirLightShadow` | 2,11 мс | 2,26 мс |
| — `FinalIntegration` | 0,233 мс | 0,233 мс |
| Поле J (rel L2 к overlay-прогону) | — | 2·10⁻⁴ (тот же решатель) |
| PSNR внутри передней грани Box | — | 37,8 дБ, средняя яркость ×0,997 |
| PSNR полного кадра | — | 27,5 дБ (небо анимировано: только небо — 19 дБ) |

Что это значит по потоку данных:
- Выигрыш ~0,7 мс — это исчезнувшая реконструкция источника в `LightScatteringCS` (4 тетраэдральные выборки
  плотности + связность stencil на фроксел). Штатная вокселизация читает то же поле аппаратной трилинейкой.
- Картинка внутри Box практически совпадает (разница сосредоточена на силуэте тумана и у пола — там, где
  overlay-реконструкция учитывает связность, а штатная трилинейка — нет). Полнокадровый PSNR не показателен:
  облака в skybox движутся между двумя снимками.
- Статус актора при инъекции: `Active B3 isotropic transport (16 directions; emissive injection via material; …)`.

Что этот A/B **не** доказывает: временное поведение (штатная история 0,9 и джиттер теперь применяются к
σs·J этого Box — отклик на смену света станет плавнее и медленнее, чем у overlay-пути), фейд после 60 %
дальности штатной вокселизации, и то, что сборка без `-BindlessAll` уже возможна — решатель по-прежнему
читает атлас плотности через bindless; инъекция снимает bindless только с потребителя (см.
`FogMS_BindlessFree_Research.md`). Визуальную оценку делает владелец проекта.

Файлы: `Tools/FogMSEnergyValidation/ProdProbe/results/abinject_r7.txt`, `results/r7_inject/*_gpuprofile.txt`.

## Раунды 8–9: пресеты качества Box и ширина групп `AngularSweepCS`

**Пресеты (коммит `4d356eb`).** У Box появился `Transport Preset`: Production = 16 направлений, ≤16 итераций,
tolerance 1e-6; High = 48 / ≤16 / 1e-8; Cinematic = 96 / 64 / 1e-14; Custom = поля редактируются вручную.
Tolerance теперь пер-Box (`Transport Tolerance`, −1 = глобальный cvar) и едет Box → ряд пакета 16.w →
`FFogMSWorldRequest::Tolerance` → решатель; из ключа истории исключён, warm start не сбрасывается.
Карты, сохранённые до этого изменения, загружаются как Custom со своими полями (вид не меняется).
Проверено на живой сцене (round 9, `Main9.log`): загруженный актор → Custom 96/16/−1, статус
`tol 1.00e-14 from cvar`; выбор Production → 16/16/1e-6; High → 48/16/1e-8, статус `48 directions, tol 1.00e-08`;
прямая правка итераций при активном пресете → Custom, значение сохранено.

**Ширина групп sweep (коммит `86a7259`, cvar `r.FogMS.Transport.SweepThreads`).** Одна группа = одно направление,
поэтому при 16 направлениях на волновых фронтах заняты лишь 16 групп. Замер на замороженной плотности,
Production-тир:

| Потоков в группе | Transport, мс | Три sweep-прохода, мс |
|---|---|---|
| 256 | 2,35 / 2,34 (повтор) | 0,659 / 0,376 / 0,338 |
| 512 | 2,23 | 0,615 / 0,326 / 0,315 |
| 1024 | 2,20 | 0,598 / 0,315 / 0,314 |

Поле J при 1024 и 256 побитово совпадает (rel L2 0). Новый дефолт — 1024 (−6 %). Вывод по потоку данных:
sweep ограничен не числом потоков, а 94 последовательными фронтами с барьером на каждом; дальнейший выигрыш
даст только другое распараллеливание (несколько направлений на группу или async compute поверх base pass).

## Раунд 10: async compute для решателя — первая проверка (`r.FogMS.Transport.AsyncCompute`, по умолчанию 0)

Что сделано (коммит `3c573a3`): при включённом cvar все чистые compute-проходы решателя (sweep, PCG, редукции,
warm start, публикация, проход инъекции 17) идут с флагом `AsyncCompute`; проходы 0/1/2/14 (чтение атласа через
bindless, inline ray tracing) остаются на графической очереди. Анализ RDG (worker): fork graphics→async
происходит после прохода 2, а join — на первом графическом потребителе результата, который сегодня стоит
**в том же хуке `PostTLASBuild`** сразу после решателя (копия `Work→Output` для overlay; access-mode проход
поля инъекции). Между хуком и `ComputeVolumetricFog` в UE 5.8 нет хука view extension, поэтому графике нечем
перекрывать async-работу. Замер это подтвердил (замороженная плотность, Production-тир, 16 напр.):

| Доставка | async 0 | async 1 | async 0 (повтор) | Поле J |
|---|---|---|---|---|
| Overlay, GPU-кадр | 11,29 мс | 11,78 мс | 11,90 мс | rel L2 1e-4 |
| Инъекция, GPU-кадр | 10,71 мс | 10,70 мс | 10,64 мс | rel L2 0 |

Разброс между повторами (±0,3 мс) больше любого эффекта; ProfileGPU видит только графическую очередь
(событий 396 вместо 532, «transport» в нём 0,55 мс — это оставшиеся на графике проходы 0/1/2/14), поэтому
стоимость async-проходов теперь измеряется только по GPU-кадру. Зависаний и ошибок RDG не было
(`Main10.log`). Вывод: чтобы спрятать ~2,2 мс, join нужно перенести за туман — публиковать результат
на кадр позже из хука `PrePostProcessPass` (двойной буфер поля / отложенная копия в резидентный атлас). Это
следующий срез; cvar остаётся выключенным.

## Раунд 11: публикация на кадр позже — решатель перекрывает свет/Lumen/туман (opt-in)

Что сделано (коммит после `b49c6c5`, «one-frame-late publication»): при `r.FogMS.Transport.AsyncCompute 1` в хуке
`PostTLASBuild` ни один графический проход не потребляет результат async-решателя. Решатель пишет только
временные текстуры графа (атлас `Work`, поле инъекции `FieldBack`); хук `PrePostProcessPass` того же графа
(после `ComputeVolumetricFog`) копирует их в резидентный атлас / volume Box и загружает соответствующий ряд 22.
Ссылки между хуками едут через RDG blackboard (живёт ровно один граф). В начале `PostTLASBuild` загружается
пара предыдущего кадра, поэтому туман читает J **на кадр позже**; пропущенная копия (разрыв, capture, сдвинутый
Box) закрывается отказом: ряд 22 = 0, поле инъекции очищается. При выключенном cvar пути прежние.

Замер (замороженная плотность, Production-тир, 16 напр., `Main11.log`):

| Доставка | async 0 | async 1 (late) | async 0 (повтор) | Δ | Поле J | Картинка в Box |
|---|---|---|---|---|---|---|
| Overlay, GPU-кадр | 11,27 мс | 10,91 мс | 11,68 мс | ≈ −0,5 мс (в пределах шума) | rel L2 1e-4 | PSNR 47 дБ |
| Инъекция, GPU-кадр | 10,67 мс | **9,58 мс** | 10,64 мс | **−1,07 мс** | rel L2 1e-4 | PSNR 38,5 дБ, яркость ×1,0001 |

В графической очереди при async остаются только проходы 0/1/2/14 (атлас через bindless, inline RT) — 0,46 мс;
`ComputeVolumetricFog` при этом дорожает (3,46 → 3,81 мс), потому что делит GPU с async-очередью, но чистый выигрыш
инъекции ≈ 1 мс из 2,2 мс решателя. Статус актора: `…; one frame late; …`. Ensure/ошибок RDG и D3D12 в логе нет.

Что не проверено: длительная стабильность (только ~3 минуты прогонов), поведение при движении Box (по дизайну —
откат к native на время перетаскивания плюс сброс истории тумана), временная задержка J на кадр при смене
света, память (временные буферы живут до `PrePostProcessPass`: ~25 МБ при 48 напр., ~50 МБ при 96).
Рекомендация: оставить cvar выключенным по умолчанию; включать вместе с Emissive Injection.

### Прогон стабильности async + инъекция (раунд 11, `soak.py`, 5 минут)

Production-тир, Emissive Injection, `r.FogMS.Transport.AsyncCompute 1`, живая анимация плотности; каждые 0,5 с камера
переставлялась по орбите, каждые ~12 с переключалась интенсивность солнца 140 ↔ 0, каждые ~20 с Box сдвигался на
15 см вверх/вниз. 483 опроса статуса — все `Active … one frame late`, откатов к native не зафиксировано (сдвиг Box
между опросами укладывался в один кадр отката), ensure/ошибок RDG и D3D12 в логе нет. Это не доказательство
отсутствия редких гонок, но первый длительный прогон пройден.

## Тиры против эталона: B2 как «Economy» (раунд 11, замороженная плотность)

Владелец отметил, что Production и Cinematic визуально почти не отличаются, а B2 и B3 очень похожи. Числа
(эталон — B3 96 направлений, 64 итерации, tolerance 1e-14; остальные — warm start, tolerance 1e-6; `tierab.sh`):

| Конфигурация | Transport, мс | GPU-кадр, мс | Поле J: rel L2 к эталону | Средняя яркость J | PSNR в Box |
|---|---|---|---|---|---|
| Эталон B3 96 / 64 | 40,2 | 49,2 | — | 1,000 | — |
| B3 24 / ≤16 | 2,83 | 11,35 | 1,2 % | 0,996 | 48 дБ |
| **B3 16 / ≤16 (Production)** | **2,21** | 10,35 | 2,2 % | 0,992 | 43 дБ |
| B2 6 / ≤16 (режим Transport) | 1,66 | 10,10 | 16 % | **1,113** | 35 дБ |
| B2 6 / ≤8 | 1,04 | 9,01 | 16 % (то же поле) | 1,113 | 36 дБ |

Что это значит по потоку данных: B2 (шесть осевых ординат) систематически завышает J на ~11 % — это смещение
квадратуры, а не недосходимость (поле при 8 и 16 итерациях совпадает, warm start сходится). Глазом это читается
как «чуть светлее», поэтому B2 и B3 кажутся похожими; относительно эталона Production-тир точнее в 7 раз.
B2 с ≤8 итерациями — кандидат в «Economy»-тир (~1 мс) для сцен, где важна цена, а не абсолютная яркость;
пока он живёт как отдельный режим `Transport`, в пресет не включён.

## Раунд 12: Box без `-BindlessAll` (injection-only runtime)

Что сделано (коммит `3975e25`, на основе `FogMS_BindlessFree_Research.md`, дополнение 2026-09-23): продюсеры
(Transport/World) читают атлас плотности обычным SRV-параметром (`FOGMS_BOUND_DENSITY_ATLAS`,
`FFogMSWorldRequest::DensityAtlas`), а не через индекс кучи; `#error` и heap-читатели оставлены только потребителям
overlay; гейты продюсера принимают любую bindless-конфигурацию с inline RT (дефолт UE 5.8 — `RayTracing`).
Без `-BindlessAll` runtime Box регистрирует view extension без текстуры пакета и дескриптора: работает только
Transport + Emissive Injection (решатель → volume инъекции → Volume-материал Box → штатный туман); резидентный
атлас, загрузка пакета, межочередные фенсы, A1d/A1e, shadow cache, BoxMode/патч движка пропускаются, остальные режимы
закрываются отказом с понятным статусом. Загрузка атласа больше не требует bindless-handle.

Проверка (`nobindless_test.sh`, редактор с одними `-d3d12 -sm6`, лог `MainNB12.log`):
- стартовый лог: `bindless configuration RayTracing, inline RT yes; available modes: injection-only …`;
- инъекция выключена → статус `Transport needs Emissive Injection or -BindlessAll … Native fog lighting with authored density`;
- инъекция включена → `Active B3 isotropic transport (16 directions, tol 1.00e-06; emissive injection via material; …) (injection-only: no BindlessAll; overlay features off)`; с `AsyncCompute 1` — `… one frame late …`;
- ensure/ошибок RDG, D3D12 и компиляции шейдеров нет.

Картинка при одинаковой камере и фиксированной фазе анимации (manual time 100 с), Production + инъекция, против того
же режима под `-BindlessAll` (`ref_bindlessall_fresh`):

| Область | Яркость, отношение | PSNR |
|---|---|---|
| Передняя грань Box | 1,0008 | 44,5 дБ |
| Ядро Box | 1,0012 | 49,4 дБ |
| Пол (слева внизу) | 0,996 | 43,2 дБ |
| Небо (анимированные облака) | 1,018 | 22 дБ |

Вывод: путь «решатель → инъекция» без `-BindlessAll` даёт ту же картинку; отличается только анимированное небо.
Первое сравнение (PSNR 18 дБ) было ошибкой методики — эталон снимался в сессии с другой камерой (внутри облака).

Следствия для продукта: (1) в упакованной игре `-BindlessAll` не парсится вообще (`RHI.cpp:1130-1166`, только
`WITH_EDITOR`), поэтому injection-only — единственный рабочий путь в билде; (2) overlay-функции (ViewIntegration,
A1d/A1e, shadow cache, SSFS sky disk, overlay-доставка B2/B3, Spatial) остаются «Advanced, editor + -BindlessAll»;
(3) `FogMS.DumpSpatial` без резидентного атласа поле не выгружает — сравнения делаются по скриншотам с фиксированной фазой.
Не проверено: длительный прогон в этой конфигурации, упакованная сборка (Simulate-in-Editor — см. раунд 13).

### Раунд 13: автозапуск runtime (без кнопки)

Коммит после `9cf4f86`: `AFogMSBoxVolume::BeginPlay` (игра/PIE) и разовый запуск на первом тике в редакторе без
`-BindlessAll` вызывают `EnableLiveBox` для включённого Transport-Box с инъекцией; с `-BindlessAll` в редакторе кнопка
остаётся (там применяется патч шейдеров движка). Проверка (`pie_test.py`, редактор без `-BindlessAll`, `MainNB13.log`):
включение инъекции в редакторе → через 2 с статус `Active … (injection-only …)` без нажатия кнопки; Simulate-in-Editor →
Box игрового мира `FogMSBoxVolume_0` активен через `BeginPlay`; ensure/ошибок нет. PIE как отдельный процесс и
упакованная сборка не проверялись.

## Раунды 14–15: ночь (пропускание солнца, SkyLight) и гибрид «баллистика/диффузия»

**Ночное свечение — две причины.** (1) Наш сборщик источников умножал солнце на пропускание атмосферы только при включённом
попиксельном режиме (`FogMS_WorldSources.cpp`, старые строки 213–214), а для солнца SkyAtmosphere `GetColor()` — это
внеатмосферная освещённость; движок применяет пропускание либо попиксельно, либо масштабируя цвет
(`DirectionalLightComponent.cpp:602-616`). Под горизонтом пропускание → 0, у нас же солнце светило в Box снизу с полной
силой. Исправлено (раунд 14): солнце берётся как `GetSunIlluminanceOnGroundPostTransmittance()`. (2) SkyLight сцены был
статическим (Real Time Capture выключен, захват сцены, интенсивность 3,4): его кубмапа остаётся дневной после заката, и
граничный член неба у нас — как и любое SkyLight-освещение штатного тумана — не темнеет.

Сумеречное сравнение (`nightcmp.py`, солнце на 8° под горизонтом, яркость облака = 75-й перцентиль кадра, эталон —
та же плотность со штатным освещением при выключенном Box):

| SkyLight | Box (overlay) / native | Box (инъекция) / native | Box (гибрид) / native | Облако, абс. |
|---|---|---|---|---|
| статический (как в сцене) | ×1,01 | — (кадр невалиден: статус лагал) | — | 168 / 166 (оба дневные) |
| **Real Time Capture** | ×1,05 | ×1,06 | ×1,07 | 113–121 (темнеет вместе с небом) |

Вывод: после правки солнца Box в сумерках отличается от штатного освещения на 5–7 %; «дневная» яркость ночью — свойство
статического SkyLight, а не переноса. Для суточного цикла SkyLight должен быть в режиме Real Time Capture (владелец включил).
Оговорка: по спектру сравнение не делалось; замер сделан при активной работе владельца в редакторе, часть кадров
фазы 1 невалидна.

**Гибрид (раунд 15, коммит `9e99cba`, материал v3 `59ac907`).** Опция Box `Hybrid Single Scattering` (требует Emissive
Injection, маркер пакета 23.w = 6, `FogMS_InjectionMode = 2`). Поток данных: проход 2 решателя дополнительно пишет
пропускание к солнцу T_sun на ячейку (тот же луч тени и тот же марш по среде, что у Direct); проход 17 публикует
RGB = J_ms = max(slab0 − slab1, 0) — полный J минус нерассеянный падающий (slab1 = `Direct + Mean(sweep kind 0)`,
т. е. солнце, локальные источники и нерассеянное граничное небо/Lumen) и A = 0,5 + 0,5·T_sun. Материал: штатное
однократное рассеяние остаётся включённым с `BaseColor = Albedo·T_sun` (фаза HG, джиттер, история, тени от геометрии —
всё штатное), `Emissive = σs·J_ms`. Известное приближение: T_sun масштабирует и штатное однократное рассеяние от неба и
локальных источников.

A/B на замороженной плотности (Production; GPU-кадр в этой сессии недостоверен — владелец параллельно нагружал GPU):

| Режим | Transport, мс | Картинка против overlay (передняя грань / ядро) |
|---|---|---|
| Overlay | 2,10 | — |
| Полная инъекция | 2,13 | 27,8 дБ / 36,7 дБ, яркость ×0,999 |
| **Гибрид** | 2,37 (+0,24 на T_sun) | 25,7 дБ / 31,1 дБ, яркость ×0,983 / ×0,991 |
| Гибрид + async (на кадр позже) | 0,48 на графике | 25,4 дБ / 31,4 дБ |

Энергия сохраняется в пределах 1–2 %; структурная разница ожидаемая (анизотропная фаза и штатные тени в прямом члене).
Визуальная оценка — за владельцем (камера в этой сессии стояла внутри облака, разница на таких кадрах почти не видна).
Не проверено: смена света при гибриде (штатная история теперь сглаживает и прямой член), локальный источник внутри
облака (T_sun гасит его однократное рассеяние), стоимость в чистом кадре.

### Раунд 16: гибрид v2 — штатному туману только солнечная доля

Отзыв владельца на v1 (раунд 15): на закате теневая сторона облака темнее, чем без гибрида, а освещённая — тусклее.
Причина в потоке данных: штатное однократное рассеяние добавляет солнце + небо + локальные источники, все с одним
множителем альбедо; масштабируя альбедо на T_sun, v1 гасила на теневой стороне и небо (которое из поля уже было
вычтено), а на солнечной стороне секторное небо с закатным ореолом подменялось SH-небом Lumen. v2: проход 2 пишет
(Direct_sun, T_sun); проход 17 публикует J − Direct_sun (небо и локальные источники остаются в поле с посекторным
самозатенением) и альфу 0,5 + 0,5·T_sun·k, где k = доля солнца в нерассеянном падающем свете ячейки (по яркости);
штатный вклад ≈ T_sun·солнце. Ночью k → 0 — гибрид вырождается в полную инъекцию. Материал не менялся.
Владелец сравнил на закате (раунд 16 установлен): «тени стали лучше», теневая сторона получила структуру, тёплая
сторона сохранила цвет. Известные приближения: k считается по яркости, а не по каналам; доля штатного SH-неба входит в k·T.

## Раунд 17: `r.FogMS.Transport.SolveInterval` и чистая стоимость гибрида

**Амортизация решателя (коммит `cab4b6a`).** Решатель считает поле раз в N кадров на вид; между ними прошлая публикация
удерживается без единого прохода (резидентный атлас, ряд 22, volume инъекции остаются как есть). Удержание разрешено,
только если предыдущий вызов опубликовал (или удержал) на прошлом кадре, warm start валиден (те же границы Box и тестовые
входы) и запрос не изменился (ревизия плотности/пакета, направления, итерации, tolerance, солнце, доставка, async);
любой отказ, разрыв, сброс истории или смена настроек → немедленный пересчёт. Инъекция удерживается только пока volume
хранит J этого вида (не очищен откатом/разрывом, в async-режиме — скопирован этим видом). Статус: `hold k/N`.
Ожидаемая цена: изменения, не меняющие ревизию (локальные источники, небо, Lumen, фаза анимации плотности), приходят
с задержкой до N−1 кадров; движение солнца меняет ревизию и считается каждый кадр.

Замер (`intervalab.sh`, замороженная плотность, Production + инъекция + гибрид, async на, однопанельная раскладка;
кадр в этой сессии в целом тяжелее прежних — окно вьюпорта крупнее):

| N | GPU-кадр (два замера), мс | Transport на графике | Картинка против N = 1 (ядро Box) |
|---|---|---|---|
| 1 | 18,5 / 15,7 (повтор 15,6 / 16,9) | 0,36 (RT-источники; решатель в async) | — |
| 2 | 14,8 / 14,7 | 0,00 на кадрах удержания | 46 дБ, ×1,002 |
| 4 | 14,2 / 13,9 | 0,00 на кадрах удержания | 44 дБ, ×0,995 |

При статичном свете поле на кадрах удержания совпадает; выигрыш ~1–2 мс кадра при N = 2–4 (в пределах шума сессии,
но монотонный). Рекомендация: N = 2 как дефолт продакшена — задержка один кадр, как у штатной истории тумана; N = 4
для дальних/фоновых Box. Дефолт пока 1.

**Гибрид, чистый GPU** (`hybridab.sh`, тот же прогон, владелец не нагружал GPU):

| Режим | Transport, мс | GPU-кадр, мс | Картинка против overlay (грань / ядро) |
|---|---|---|---|
| Overlay | 2,95 (первый прогон, прогрев) | 20,2 | — |
| Полная инъекция | 2,42 | 16,45 | 27,1 дБ / 43,4 дБ, ×1,000 / ×1,006 |
| Гибрид v2 | 2,44 | 16,42 | 25,0 дБ / 31,2 дБ, ×1,015 / ×1,019 |
| Гибрид v2 + async | 0,35 на графике | 18,2 (шум) | 24,6 дБ / 31,5 дБ |

Гибрид стоит столько же, сколько полная инъекция (T_sun считается тем же лучом, что и Direct; +0,02 мс). По энергии
гибрид v2 на 1,5–2 % ярче overlay внутри Box (v1 был на 1–2 % темнее): вклад k·T·(SH-небо) штатного пути плюс наше
секторное небо в поле частично перекрываются. Это верхняя оценка двойного счёта неба на солнечной стороне.

Шум инструментов: два ensure `EditorViewportClient.cpp: !bCheckMissingOverride || bRemoved` в логе сессии — это
`editor_set_viewport_realtime(True)` из скрипта замера (override без снятия), к плагину отношения не имеет.

## Раунд 18: Runtime-модули и публичный API рендерера (сборка игровой цели)

Коммиты `649b8ed` (модули), `d33f9bf` + `410ac41` (публичный API). `MultiLobeSpec` и `FogMSRender` — теперь `Runtime`-модули
с `PlatformAllowList: Win64` (`FogMSRender` на `PostConfigInit` ради маппинга каталога шейдеров); патчер шейдеров движка,
raw-AO overlay, тост и `FEditorSupportDelegates` — только под `WITH_EDITOR`, в игре их заглушки отказывают; вне редактора Box
всегда injection-only. Продюсер больше не использует `FViewInfo`: TLAS — `FXRenderingUtils::RayTracing::GetRayTracingSceneViewRDG`,
binding data — виртуальный `FSceneView::GetInlineRayTracingBindingDataBuffer`, ключ вида/кадр — `FSceneView`/`FSceneViewFamily`,
цвет SkyLight — `View.SkyLightColor` в шейдере; флаги CastShadow на сегмент собираются из публичных видимых binding’ов
(`r.FogMS.Transport.PublicHitFlags`, по умолчанию 1; приватный буфер оставлен как 0 для A/B); атлас плотности без
`-BindlessAll` — обычная RHI-текстура без нативного D3D12-ресурса.

**Проверено:** `RunUAT BuildPlugin -StrictIncludes` теперь собирает три цели — `UnrealEditor` и две `UnrealGame`
(Development, Shipping) — все `Succeeded` (`Build18.log`); единственная поломка была недостающим `ShaderCompilerCore.h`,
который раньше приходил транзитивно через приватный `SceneRendering.h`.

**A/B `PublicHitFlags` (раунд 18 установлен, `hitflagsab.sh`, замороженная сцена, overlay):** поле с публичными флагами
на 1,3 % темнее приватного буфера (rel L2 0,012, средняя яркость ×0,987; скриншот 27 дБ). Значит, наш сборщик флагов
не тождествен движковому — вероятные причины: значение по умолчанию для сегментов без binding’а («отбрасывает тень»),
геометрия, отсутствующая в публичном списке видимых binding’ов, или расхождение в индексации hit group. Дефолт cvar
возвращён на 0 (приватный буфер) до выяснения; расследование поручено отдельному worker’у.

**Не проверено ранее:** тождественность поля при `PublicHitFlags 1` против 0 (ожидается совпадение, кроме динамических мешей с
выключенными RT-тенями — публичный список binding’ов их не содержит при `r.RayTracing.ParallelMeshBatchSetup=1`;
скрипт `hitflagsab.sh`); поведение в реальной упаковке (остаются блокеры: атлас плотности читает editor-only Source
текстуры, обязательные cvar `r.RayTracing.Culling=0`/`r.Lumen.AsyncCompute=0`, гейт источника Lumen ровно 5.8.2 —
в работе). Остающиеся приватные зависимости: источник Lumen (P8), кубмапа неба/список источников (P9/P11), SSFS-постфильтр,
Spatial-превью — см. `FogMS_Fab_Readiness.md`.

## Что не сделано / открыто

- Квадратура, ориентированная на солнце: работает после исключения диска и префильтрации неба, включена по умолчанию.
- Кэш связности stencil — P2 аудитора, не начат (при Emissive Injection реконструкция в LightScatteringCS не используется, актуальность ниже). `AngularSweepCS`: ширина группы измерена (раунд 9), дальнейший выигрыш — только другое распараллеливание.
- Требование `-BindlessAll` и crash-guard — исследование завершено (`FogMS_BindlessFree_Research.md`). Доставка через emissive volume-материала реализована как опция Box (раунд 7, см. выше): поле совпадает, LightScattering дешевле на ~0,7 мс; остаётся перевести входы решателя (атлас плотности, публикация) с bindless на обычные привязки, чтобы снять `-BindlessAll` целиком.
- Спайк при остановке камеры в редакторе: причина найдена (hit proxy + синхронное чтение GPU, ~87 мс базово + слив GPU), в игре отсутствует; уменьшается пропорционально GPU-кадру.
